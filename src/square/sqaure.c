#include <string.h>
#include "square.h"
int square(char syl[],int line){
  if(strcmp(syl,"default")==0)
    //call default
  else if(strcmp(sql,horizontal)==0)
    //call horizontal
  else
    return 0;
}
