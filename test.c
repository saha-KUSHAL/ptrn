#include<stdio.h>
#include"src/triangle/triangle.h" //replace with the right header
int main(void){
  int line;
  printf("\nEnter no of lines:");
  scanf("%d",&line);
  t_vertical(line); //replace the function with the test function
}
